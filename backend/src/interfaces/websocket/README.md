# WebSocket (Fase futura)

Não implementado na Fase 2.

Rotas planejadas:

- `/ws/admin` — painel administrativo (Angular Admin)
- `/ws/client` — escala operacional (Angular Cliente)

Eventos previstos: ver `infrastructure/websocket/channels.ts`.

Habilitar com `WS_ENABLED=true` quando a Fase de tempo real for iniciada.
